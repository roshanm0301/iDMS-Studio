# Tasks — M5

1. Create EntitySelector component (dropdown from MOCK_ENTITIES)
2. Create FieldPicker with search, type filter, multi/single select
3. Create FieldMetadataBadge (fieldType, isRequired, isSystem, isComputed indicators)
4. Create FieldCompatibilityWarning for incompatible field targets
5. Add field compatibility filter logic (e.g. computed fields not editable in forms)
6. Wire FieldPicker into SmartCrud, form config, grid config contexts
7. Add tests for FieldPicker search and selection
8. Run milestone gate checks and update HANDOFF.md
