# Tasks — M3

1. Create SmartCrudEntitySelector (dropdown from MOCK_ENTITIES)
2. Create SmartCrudAutoGenerateButton (generates columns from entity fields)
3. Create SmartCrudColumnList with drag-to-reorder and remove
4. Create SmartCrudFormFieldList with drag-to-reorder and remove
5. Create SmartCrudPanel orchestrating above components
6. Wire entity selection to field list generation (exclude system/computed fields from form)
7. Store configuration in ViewArtifact components/bindings
8. Add SmartCrud preview panel (shows table + form preview)
9. Add tests for auto-generation and field list management
10. Run milestone gate checks and update HANDOFF.md
