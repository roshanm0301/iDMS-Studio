# Tasks — M11

1. Create DataSourceRegistryPanel (list all data sources for the view)
2. Create DataSourceConfigurator (type: primary / related / static / mock-api, entity binding)
3. Create FieldLookupOverrideConfigurator (per-field: override data source + filter expression)
4. Create ViewFilterOverrideConfigurator (view-level filter expression, plain text)
5. Create DataSourcePreview (runs mock filter and shows matching records)
6. Implement mock filter evaluator (simple field=value matching on mock records)
7. Wire demo scenarios: Product default, vehicle-only override, branch-filtered override
8. Store data sources in ViewArtifact.dataSources
9. Add tests for mock filter evaluator and data source configuration
10. Run milestone gate checks and update HANDOFF.md
