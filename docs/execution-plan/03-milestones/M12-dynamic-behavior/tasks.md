# Tasks — M12

1. Create BehaviorRuleListPanel with add/remove/reorder
2. Create ConditionBuilder (condition type selector + condition value input)
3. Create TargetSelector (dropdown: all fields, sections, tabs, grid columns, actions in view)
4. Create EffectSelector (show/hide/enable/disable/required/optional/readonly/editable)
5. Create BehaviorRuleEditor composing condition + target + effect
6. Create MockRuleEvaluator in lib/ui-studio/ (pure function, no side effects)
7. Create BehaviorSimulationPanel (set role, field values, workflow state, device, see effects)
8. Wire rules to ViewArtifact.behaviorRules
9. Add demo rules: PaymentType=Finance shows Financer field, Admin role shows Delete action
10. Add tests for MockRuleEvaluator
11. Run milestone gate checks and update HANDOFF.md
