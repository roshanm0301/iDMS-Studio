# Tasks — M10

1. Create LookupConfigPanel component
2. Create LookupEntitySelector (dropdown from MOCK_ENTITIES)
3. Create LookupDisplayFieldSelector (fields from selected target entity)
4. Create LookupValueFieldSelector (ID/key fields from target entity)
5. Create LookupPickerColumnsConfigurator (select which columns appear in picker)
6. Create LookupSearchFieldsSelector (fields included in search)
7. Create LookupDefaultFilterExpression input (plain text, no parser needed)
8. Create MockLookupPreview (simulated picker dialog with mock records)
9. Wire Customer and Product lookup demo scenarios
10. Store config in ViewArtifact.components[field].lookupConfig
11. Add tests for LookupConfigPanel and MockLookupPreview
12. Run milestone gate checks and update HANDOFF.md
