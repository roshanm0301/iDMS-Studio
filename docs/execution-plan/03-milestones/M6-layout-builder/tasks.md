# Tasks — M6

1. Create LayoutBuilderPanel with container tree
2. Create LayoutCanvas that renders LayoutDefinition as preview
3. Create SectionConfigurator (label, collapsible, column count)
4. Create TabsConfigurator (tab labels, active tab)
5. Create ColumnConfigurator (column count, spans)
6. Create AccordionConfigurator (sections with expand/collapse)
7. Wire drag-to-reorder for containers
8. Wire field assignment to containers (from FieldPicker M5)
9. Store layout in ViewArtifact.layout
10. Add tests for layout metadata mutations
11. Run milestone gate checks and update HANDOFF.md
