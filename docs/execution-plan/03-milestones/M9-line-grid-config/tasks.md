# Tasks — M9

1. Create LineGridConfigPanel with column list and add/remove/reorder
2. Create LineGridColumnConfigurator (field, label, width, editable, required, renderer)
3. Create LineGridPreview with mock SaleOrderLine sample records
4. Add editable cell simulation (input fields in preview mode)
5. Add add-row and delete-row behavior flags to metadata
6. Add aggregate/total row placeholder configuration
7. Wire line entity binding to ViewArtifact.layout (line grid section)
8. Integrate with TransactionWorkspaceCanvas (M4) for full preview
9. Add tests for column config and preview rendering
10. Run milestone gate checks and update HANDOFF.md
