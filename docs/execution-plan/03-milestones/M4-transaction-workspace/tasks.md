# Tasks — M4

1. Create TransactionWorkspacePanel with header/line entity selectors
2. Create relationship mapper (maps header.id to line.headerId)
3. Create HeaderSectionsConfigurator (sections with header entity fields)
4. Create LineGridColumnsConfigurator (columns from line entity fields)
5. Create TotalsPanelConfigurator (computed fields from header entity)
6. Create ViewCodeInput (captured as ViewArtifact.viewCode)
7. Create TransactionWorkspaceCanvas preview
8. Wire SaleOrder + SaleOrderLine as primary demo scenario
9. Add tests for entity selection and metadata storage
10. Run milestone gate checks and update HANDOFF.md
