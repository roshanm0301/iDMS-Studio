# Tasks — M8

1. Create FormFieldConfigPanel
2. Create WidgetTypeSelector (11 widget types)
3. Create FieldPresentationEditor (label override, placeholder, help text)
4. Create FieldBehaviorToggle (required, read-only, display-only, editable)
5. Wire field config to ViewArtifact.components metadata
6. Show incompatible widget choices blocked/warned
7. Wire section placement to LayoutDefinition
8. Add tests for widget type selection and behavior toggles
9. Run milestone gate checks and update HANDOFF.md
