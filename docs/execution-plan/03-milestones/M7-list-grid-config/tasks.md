# Tasks — M7

1. Create GridConfigurationPanel
2. Create GridColumnConfigurator (per-column: label, width, sortable, filterable, renderer)
3. Create ColumnRendererSelector (badge, text, currency, date, boolean)
4. Create GridPreview using TanStack Table with mock sample records
5. Wire column configuration to ViewArtifact components metadata
6. Add reorder (drag or up/down arrows)
7. Flag invalid field references
8. Add tests for column configuration and preview
9. Run milestone gate checks and update HANDOFF.md
