# Tasks — M14

1. Create GridCellEventListPanel with add/remove events
2. Create GridCellTriggerSelector (column dropdown from line grid columns)
3. Create GridCellActionBuilder (action type + target column + value/formula input)
4. Implement all 6 action types as metadata
5. Create MockCellEventSimulator (evaluates events on mock row data in preview)
6. Wire Product change demo: populate UOM, Rate, Tax from MOCK_PRODUCTS lookup
7. Wire Qty/Rate change demo: recalculate Amount column
8. Wire Discount threshold demo: warn if discount > 30%
9. Store cell events in ViewArtifact metadata (gridEvents or behaviorRules subcategory)
10. Add tests for MockCellEventSimulator
11. Run milestone gate checks and update HANDOFF.md
