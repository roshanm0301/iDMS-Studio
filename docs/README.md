# iDMS Metadata Implementation Docs

This folder tracks implementation-facing documentation for the contract-first iDMS metadata platform.

The authoritative source documents at the repository root and under `Phase_wise_requirements/` define the requirements. This folder records the repo-local guardrails, implementation backlog, and test expectations used while replacing the current screen-level implementation.
